BrewVimEmacs

# install homebrew
ruby -e 
   ”$(curl -fsSL https://
      raw.githubusercontent.com/Homebrew/install/master/install)”

brew install macvim —-with-cscope —-with-lua —-override-system-vim
brew linkapps # this is the same with 
# ln -s /usr/local/Cellar/macvim/[version]/MacVim.app /Applications

brew install --cocoa emacs
brew linkapps 

# install Exuberant ctags
brew install ctags

